[profile.default]
src = "src"
out = "out"
libs = ["lib"]
solc_version = "0.8.30"
optimizer = true
optimizer_runs = 200

[fuzz]
runs = 1024

[rpc_endpoints]
# never public RPCs — Alchemy only (key via env)
base = "https://base-mainnet.g.alchemy.com/v2/${ALCHEMY_API_KEY}"
